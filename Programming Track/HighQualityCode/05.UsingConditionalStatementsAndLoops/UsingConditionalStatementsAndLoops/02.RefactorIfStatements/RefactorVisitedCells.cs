using System;
using System.Linq;

//The if clause is commented(otherwise in solution will pop up Errors)

namespace _02.RefactorIfStatements
{
    class RefactorVisitedCells
    {
        //bool isXInRange= x >= MIN_X && x =< MAX_X;
        //bool isYInRange= MAX_Y >= y && MIN_Y <= y;
        //if (isXInRange && isYInRange && shouldVisitCell)
        //{
        //   VisitCell();
        //}
    }
}
